    
 
Класс: 

    - QOpenGLWidget - является контекстом рисования

        "Make your class inherit from it and use the subclass like any other QWidget"

    #include <QOpenGLWidget>

    find_package(Qt6 REQUIRED COMPONENTS OpenGLWidgets)
    target_link_libraries(mytarget PRIVATE Qt6::OpenGLWidgets)

        "QOpenGLWidget provides three convenient virtual functions that you can reimplement 
         in your subclass to perform the typical OpenGL tasks:"

Функции: 

    virtual void	initializeGL() - "Sets up the OpenGL resources and state. 
                                      Gets called once before the first time 
                                      resizeGL() or paintGL() is called."

    virtual void	resizeGL(int w, int h) - "Sets up the OpenGL viewport, projection, etc. 
                                              Gets called whenever the widget has been resized 
                                              (and also when it is shown for the first time because 
                                              all newly created widgets get a resize event automatically)."



    virtual void	paintGL()      - "Renders the OpenGL scene. 
                                      Gets called whenever the widget needs to be updated." 
                                
                                   - цикл отрисовки

---------------------------------------------------------------------------------------------------
                
    initializeGL() {

            glEnable(GL_DEPTH_TEST) - включаем буфер глубины для отображения Z-координаты
                                    - "If enabled, do depth comparisons and update the depth buffer."
    }

    resizeGL(w, h) {

        glViewPort(0, 0, width, height) - нужно ли задавать при фиксированном размере?
                                        - "внутри функции, которая вызывается у вас при 
                                           изменение размера окна resizeGL(w, h)"
    }

    paintGL() {
        
    }

